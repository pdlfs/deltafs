
# if you have depends, uncommend and add them: "find_dependency(Foo 2.4.1)"
include (CMakeFindDependencyMacro)

find_dependency(deltafs-common REQUIRED)

include ("${CMAKE_CURRENT_LIST_DIR}/deltafs-targets.cmake")

# could include a macros file if one is used

